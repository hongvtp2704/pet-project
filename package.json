{
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently yarn:dev:server yarn:dev:client",
    "dev:server": "cd ./server && concurrently 'vite' 'nodemon src/server.ts'",
    "dev:client": "cd ./client && vite"
  }
}
